[Unit]
Description=TV web server (python http.server)
After=network.target

[Service]
Type=simple
User=jw
WorkingDirectory=/home/jw/src/tv
ExecStart=/usr/bin/python -m http.server 8765 --bind 0.0.0.0
Restart=always
RestartSec=2

[Install]
WantedBy=multi-user.target
