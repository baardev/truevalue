---
description: Treat Markdown as the only first-class project documentation for context
alwaysApply: true
---

# Markdown-only codebase context

- Treat `**/*.md` as the authoritative textual context for this repository unless the user explicitly attaches other paths.
- Do not assume availability of non-Markdown source files in semantic search or `@` file references; `.cursorignore` restricts them.
