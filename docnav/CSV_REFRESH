#!/usr/bin/env sh
# Regenerate docnav/csv_catalog.json from CSV files under frontend/project/.
# Run from the repository root:  sh docnav/CSV_REFRESH

set -e
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

python3 docnav/docnav_build_csv_catalog_json.py
